# コード地図（自動生成）— index.html

> このファイルは `generate_codemap.py` が `index.html` を解析して自動生成したものです。手で編集せず、コードを変えたら再生成してください。

- 生成日時：2026-07-21 16:30
- 総行数：8,162 行
- セクション数：40
- 関数（function宣言）数：270

## 1. セクション目次

各セクションの開始行と、含まれる関数の数。行番号はクリックの目印に。

| # | 開始行 | セクション | 関数数 |
|---|-------|-----------|-------|
| 1 | 1848 | IndexedDB 定数 | 0 |
| 2 | 1858 | ストア定義 | 4 |
| 3 | 2019 | DB接続の共通処理 | 2 |
| 4 | 2061 | projects ストアのCRUD | 3 |
| 5 | 2103 | schedules ストアのCRUD | 5 |
| 6 | 2178 | tasks ストア（カンバン）のCRUD | 4 |
| 7 | 2229 | comments ストアのCRUD | 4 |
| 8 | 2286 | changelog ストア（変更履歴）のCRUD | 3 |
| 9 | 2351 | issues ストア（マインドマップ）のCRUD | 4 |
| 10 | 2404 | memos ストアのCRUD | 4 |
| 11 | 2455 | quickmemos ストア（即時メモ）のCRUD | 5 |
| 12 | 2534 | snapshots ストアのCRUD | 4 |
| 13 | 2587 | 複合操作（複数ストアにまたがる処理）の共通ユーティリティ | 7 |
| 14 | 2817 | H1：データ出力（JSONエクスポート） | 4 |
| 15 | 2879 | H2：データ取込（JSONインポート＋差分適用） | 12 |
| 16 | 3195 | H3：PDF出力（exportPDF） | 4 |
| 17 | 3308 | UI：状態・純粋関数（DOM非依存） | 11 |
| 18 | 3552 | 追加実装仕様書1章：営業日・祝日カレンダー | 4 |
| 19 | 3630 | H4：Excel出力（exportExcel） | 15 |
| 20 | 3992 | 追加実装仕様書8.3節：マインドマップの自動レイアウト計算 | 10 |
| 21 | 4166 | 追加実装仕様書8.3節：画面フィット | 12 |
| 22 | 4754 | UI：DOM反映（薄い関数） | 7 |
| 23 | 4801 | UI：統括（データ取得→純粋関数→DOM反映のつなぎ） | 7 |
| 24 | 4946 | P2：スナップショットパネル | 11 |
| 25 | 5231 | P3：即時メモパネル | 6 |
| 26 | 5321 | P4：メモパネル | 11 |
| 27 | 5525 | P5：コメント一覧パネル | 8 |
| 28 | 5660 | P6：変更履歴パネル | 14 |
| 29 | 5907 | サイドパネルのリサイズハンドル（UI差分表No.76） | 4 |
| 30 | 5971 | 共通UI：トースト通知（UIデザイン仕様書2.4節） | 1 |
| 31 | 5998 | UI：モーダル（モーダル・ダイアログ一覧①：中央モーダル） | 6 |
| 32 | 6078 | M3：マイルストーン管理モーダル | 24 |
| 33 | 6747 | ガントバーのドラッグによる日程変更（詳細設計書3.2.4） | 4 |
| 34 | 6917 | ガントバー直接クリックの軽量ポップアップ（UI差分表No.75／対応表No.17） | 10 |
| 35 | 7118 | UI：その他のイベントハンドラ | 8 |
| 36 | 7302 | 追加実装仕様書8.1節：マインドマップのインライン編集 | 5 |
| 37 | 7414 | マインドマップ：ノードのドラッグ配置 | 3 |
| 38 | 7487 | マインドマップ：右クリックメニュー | 10 |
| 39 | 7642 | WBSパネルとガント本体の縦スクロール同期（対応表No.41） | 2 |
| 40 | 7676 | ガント背景ドラッグによる自動スクロール（対応表No.42） | 8 |

## 2. 関数カタログ（セクション別）

「目的」はコード内コメント（【設計判断】【結果】等）から機械抽出したもの。「証明構造」は 前提→処理→結果 のコメントが揃っている関数の印。

> **「直接触るストア」の読み方**：`STORE_xxx` 定数を関数の本文で直接名指ししているストアだけを表示している。`deleteScheduleCascade` のような複合操作が「—」になるのは、ストアを直接触らず CRUD 関数を組み合わせて実現しているため（＝組み立て層）。この「—」は、CRUD層と組み立て層が分離している事実を表す。

### ストア定義

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 1919 | `createStore` |  | db 上に storeDefinition.name のストアと、指定インデックスが作成されていることを保証する。 | — | — | ○ |
| 1937 | `deleteDatabase` |  | 成功時：開いた IDBDatabase を resolve する。 | — | — | ○ |
| 1954 | `openDBOnce` |  | 成功時：開いた IDBDatabase を resolve する。 | — | — | ○ |
| 2003 | `openDB` | async | 成功時：開いた IDBDatabase を resolve する（必要なら初期化を経て）。 | — | — | ○ |

### DB接続の共通処理

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2028 | `getDB` | async | 開いている IDBDatabase を1つ返すことを保証する（毎回同じインスタンス）。 | — | — | ○ |
| 2047 | `promisifyRequest` |  | 成功時：request.result を resolve。失敗時：発生したエラーを reject。 | — | — | ○ |

### projects ストアのCRUD

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2073 | `addProject` | async | コミット完了後に resolve する（＝保存が確定したことを保証）。 | projects | データモデル設計 | ○ |
| 2085 | `getAllProjects` | async | プロジェクトの配列を resolve する（0件なら空配列）。 | projects | データモデル設計 | ○ |
| 2096 | `deleteProject` | async | コミット完了後に resolve する（対象が存在しなくても delete はエラーにならない＝冪等）。 | projects | データモデル設計 | ○ |

### schedules ストアのCRUD

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2116 | `addSchedule` | async | コミット完了後に resolve する（＝保存が確定したことを保証）。 | schedules | データモデル設計 | ○ |
| 2129 | `getSchedulesByProject` | async | スケジュールの配列を resolve する（0件なら空配列）。ルート行・子行の区別はせず全行を返す。 | schedules | — | ○ |
| 2144 | `getChildSchedules` | async | 子スケジュールの配列を resolve する（子がなければ空配列）。 | schedules | 基本設計書・データモデル設計・シーケンス図集 | ○ |
| 2159 | `deleteSchedule` | async | コミット完了後に resolve する（対象が存在しなくても delete はエラーにならない＝冪等）。 | schedules | 基本設計書・データモデル設計・シーケンス図集 | ○ |
| 2171 | `getAllSchedules` | async | スケジュールの配列を resolve する（0件なら空配列）。 | schedules | — | ○ |

### tasks ストア（カンバン）のCRUD

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2191 | `addTask` | async | コミット完了後に resolve する（＝保存が確定したことを保証）。 | tasks | データモデル設計 | ○ |
| 2202 | `getTasksByProject` | async | タスクの配列を resolve する（0件なら空配列）。カンバン列ごとの振り分けは呼び出し側の責務。 | tasks | — | ○ |
| 2212 | `deleteTask` | async | コミット完了後に resolve する（対象が存在しなくても delete はエラーにならない＝冪等）。 | tasks | — | ○ |
| 2222 | `getAllTasks` | async | タスクの配列を resolve する（0件なら空配列）。 | tasks | — | ○ |

### comments ストアのCRUD

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2241 | `addComment` | async | コミット完了後に resolve する（＝保存が確定したことを保証）。 | comments | データモデル設計 | ○ |
| 2255 | `getCommentsByTask` | async | コメントの配列を resolve する（0件なら空配列）。 | comments | 基本設計書・データモデル設計 | ○ |
| 2268 | `deleteComment` | async | コミット完了後に resolve する（対象が存在しなくても delete はエラーにならない＝冪等）。 | comments | 基本設計書・データモデル設計 | ○ |
| 2279 | `getAllComments` | async | コメントの配列を resolve する（0件なら空配列）。 | comments | — | ○ |

### changelog ストア（変更履歴）のCRUD

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2299 | `addChangelogEntry` | async | コミット完了後に resolve する（＝保存が確定したことを保証）。 | changelog | データモデル設計 | ○ |
| 2311 | `getChangelogByProject` | async | 変更履歴の配列を resolve する（0件なら空配列）。 | changelog | 詳細設計書 | ○ |
| 2330 | `clearChangelogByProject` |  | コミット完了後に resolve する（対象が0件でも正常に完了する）。 | changelog | 詳細設計書 | ○ |

### issues ストア（マインドマップ）のCRUD

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2361 | `addIssue` | async | コミット完了後に resolve する（＝保存が確定したことを保証）。 | issues | データモデル設計 | ○ |
| 2374 | `getIssuesByProject` | async | ノードの配列を resolve する（0件なら空配列）。親子どちらのノードも区別せず全件返す。 | issues | 詳細設計書・データモデル設計 | ○ |
| 2387 | `deleteIssue` | async | コミット完了後に resolve する（対象が存在しなくても delete はエラーにならない＝冪等）。 | issues | 詳細設計書 | ○ |
| 2397 | `getAllIssues` | async | ノードの配列を resolve する（0件なら空配列）。 | issues | — | ○ |

### memos ストアのCRUD

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2416 | `addMemo` | async | コミット完了後に resolve する（＝保存が確定したことを保証）。 | memos | — | ○ |
| 2427 | `getMemosByProject` | async | メモの配列を resolve する（0件なら空配列）。 | memos | 詳細設計書 | ○ |
| 2438 | `deleteMemo` | async | コミット完了後に resolve する（対象が存在しなくても delete はエラーにならない＝冪等）。 | memos | 詳細設計書 | ○ |
| 2448 | `getAllMemos` | async | メモの配列を resolve する（0件なら空配列）。 | memos | — | ○ |

### quickmemos ストア（即時メモ）のCRUD

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2465 | `addQuickMemo` | async | コミット完了後に resolve する（＝保存が確定したことを保証）。 | quickmemos | — | ○ |
| 2476 | `getQuickMemosByProject` | async | 即時メモの配列を resolve する（0件なら空配列）。 | quickmemos | — | ○ |
| 2487 | `deleteQuickMemo` | async | コミット完了後に resolve する（対象が存在しなくても delete はエラーにならない＝冪等）。 | quickmemos | 詳細設計書 | ○ |
| 2503 | `clearQuickMemosByProject` |  | コミット完了後に resolve する（対象が0件でも正常に完了する）。 | quickmemos | 詳細設計書 | ○ |
| 2527 | `getAllQuickMemos` | async | 即時メモの配列を resolve する（0件なら空配列）。 | quickmemos | — | ○ |

### snapshots ストアのCRUD

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2546 | `addSnapshot` | async | コミット完了後に resolve する（＝保存が確定したことを保証）。 | snapshots | — | ○ |
| 2557 | `getSnapshotsByProject` | async | スナップショットの配列を resolve する（0件なら空配列）。 | snapshots | 詳細設計書 | ○ |
| 2570 | `deleteSnapshot` | async | コミット完了後に resolve する（対象が存在しなくても delete はエラーにならない＝冪等）。 | snapshots | 詳細設計書 | ○ |
| 2580 | `getAllSnapshots` | async | スナップショットの配列を resolve する（0件なら空配列）。 | snapshots | — | ○ |

### 複合操作（複数ストアにまたがる処理）の共通ユーティリティ

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2597 | `generateId` |  | 十分に一意な文字列idを返す。 | — | 基本設計書・データモデル設計 | ○ |
| 2614 | `diffFields` |  | [{field, label, before, after}] の配列を返す（1件も変わっていなければ空配列）。 | — | 基本設計書 | ○ |
| 2635 | `recordChangelogEntry` | async | 記録した場合はtrueを、"edit"で変更が無く記録しなかった場合はfalseを返す。 | — | 基本設計書 | ○ |
| 2660 | `getScheduleById` | async | 見つかった場合はそのオブジェクトを、存在しない場合は undefined を resolve する。 | schedules | 基本設計書・詳細設計書 | ○ |
| 2682 | `syncAncestorDates` | async | 祖先日付の再計算をなぜ独立した関数にするか | — | 基本設計書・詳細設計書・データモデル設計・シーケンス図集 | ○ |
| 2717 | `deleteScheduleCascade` | async | スケジュールの連鎖削除 | — | 基本設計書・詳細設計書・データモデル設計・シーケンス図集 | ○ |
| 2788 | `deleteIssueCascade` | async | マインドマップノードの連鎖削除 | — | 詳細設計書 | ○ |

### H1：データ出力（JSONエクスポート）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2830 | `collectAllStoresDataForExport` | async | {projects, schedules, tasks, issues, comments, memos, quickmemos, snapshots} を resolve する。 | — | 基本設計書 | ○ |
| 2848 | `buildExportBackupFileName` |  | 組み立てたファイル名の文字列を返す。 | — | 基本設計書 | ○ |
| 2858 | `downloadObjectAsJsonFile` |  | ブラウザのダウンロードが実行される（戻り値なし）。 | — | 基本設計書・詳細設計書 | ○ |
| 2872 | `handleExportDataButtonClick` | async | ダウンロード実行後、トースト「データをエクスポートしました」を表示する。 | — | 基本設計書・詳細設計書 | ○ |

### H2：データ取込（JSONインポート＋差分適用）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 2894 | `getBackupStoreDefinitions` |  | 8ストア分の { key, label, getAll, put, del, nameField, logged, fieldLabels } の配列を返す。 | — | 基本設計書 | ○ |
| 2910 | `isValidImportedBackupShape` |  | 有効な形をしていればtrue、そうでなければfalseを返す。 | — | 基本設計書 | ○ |
| 2923 | `computeStoreDiff` |  | { add: [record...], mod: [{before, after}...], del: [record...] } を返す。 | — | — | ○ |
| 2954 | `buildImportDiff` | async | [{ key, label, nameField, add, mod, del }, ...]（差分のあるストアのみ）を返す。 | — | モーダル・ダイアログ一覧 | ○ |
| 2972 | `renderImportDiffItemToHtml` |  | <label>要素のHTML文字列を返す。 | — | 基本設計書・モーダル・ダイアログ一覧 | ○ |
| 2990 | `renderImportDiffModalToHtml` |  | モーダルパネルにそのまま入れられるHTML文字列を返す。 | — | 基本設計書・モーダル・ダイアログ一覧 | ○ |
| 3038 | `openImportDiffModal` |  | モーダルが操作可能な状態で表示される。 | — | — | ○ |
| 3067 | `applyOneImportItem` | async | 1件の適用（put/delete、必要なら変更履歴の記録）が完了した時点で resolve する。 | — | 詳細設計書 | ○ |
| 3098 | `handleApplyImportButtonClick` | async | 適用後はモーダルを閉じ、プロジェクト一覧・現在の画面を再読込し、 | — | 基本設計書・詳細設計書 | ○ |
| 3151 | `handleImportFileInputChange` | async | 検証NG時は何も変更せず終了する。OK時は差分比較モーダルを開く。 | — | 基本設計書 | ○ |
| 3181 | `buildReportFileName` |  | 組み立てたファイル名の文字列を返す。 | — | 基本設計書 | ○ |
| 3190 | `formatDateToOutputTimestampString` |  | 組み立てた文字列を返す。 | — | 基本設計書 | ○ |

### H3：PDF出力（exportPDF）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 3220 | `arePdfLibrariesLoaded` |  | 両方読み込まれていればtrue、そうでなければfalseを返す。 | — | 詳細設計書 | ○ |
| 3230 | `captureElementFullContentToCanvas` |  | キャプチャ結果のCanvas要素を resolve する。 | — | 基本設計書 | ○ |
| 3245 | `composeGanttPdfCanvas` |  | 合成済みのCanvas要素を返す。 | — | 基本設計書・詳細設計書 | ○ |
| 3277 | `exportPDF` | async | 成功時はトースト「PDFを保存しました」、ライブラリ未読込時は | — | 詳細設計書 | ○ |

### UI：状態・純粋関数（DOM非依存）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 3372 | `escapeHtmlText` |  | そのままHTML文字列に埋め込んでよいエスケープ済み文字列を返す。 | — | 基本設計書・UIデザイン仕様書 | ○ |
| 3396 | `buildScheduleTreeRows` |  | ガントチャートの行として上から順に描画できる配列 | — | 基本設計書・UIデザイン仕様書 | ○ |
| 3417 | `appendScheduleAndDescendants` |  | — | — | — |  |
| 3442 | `calculateWbsNumbers` |  | スケジュールid→WBS番号文字列 のMapを返す。 | — | — | ○ |
| 3459 | `filterVisibleScheduleTreeRows` |  | 実際に画面へ表示すべき行だけの配列を返す（WBS番号は事前に計算済みのものを使う | — | 対応表 | ○ |
| 3482 | `areAllParentRowsCollapsed` |  | 親行がすべて折りたたまれていればtrue、そうでなければfalseを返す。 | — | 対応表 | ○ |
| 3490 | `groupTasksByStatus` |  | カンバンの3列分のタスク配列 {backlog, doing, done} を返す。 | — | — | ○ |
| 3502 | `parseDateStringToUtcTimestamp` |  | その日付のUTC深夜0時のミリ秒タイムスタンプを返す。 | — | 基本設計書 | ○ |
| 3514 | `buildGanttTimelineDays` |  | タイムライン列として左から順に描画できる配列を返す。 | — | 基本設計書 | ○ |
| 3537 | `calculateDayOffsetFromTimelineStart` |  | 経過日数（整数）。範囲外の日付なら負の値、またはタイムラインを超える大きな値になる。 | — | — | ○ |
| 3546 | `formatUtcTimestampToDateString` |  | UTC基準の日付文字列を返す。 | — | — | ○ |

### 追加実装仕様書1章：営業日・祝日カレンダー

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 3583 | `isJapaneseHoliday` |  | 祝日ならtrue。 | — | — | ○ |
| 3591 | `calculateDayType` |  | "holiday" \| "sunday" \| "saturday" \| null を返す。 | — | 対応表 | ○ |
| 3602 | `calculateDayTypeBackgroundClassName` |  | 対応するCSSクラス名。dayTypeがnull（平日）なら空文字列。 | — | 対応表 | ○ |
| 3615 | `calculateBusinessDayCount` |  | 営業日数（整数）。 | — | — | ○ |

### H4：Excel出力（exportExcel）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 3664 | `flattenScheduleTreeRowsForExcel` |  | [{no, wbsNumber, schedule, depthLevel, depthLabel, durationDays}, ...]（表示順は | — | 基本設計書 | ○ |
| 3686 | `calculateExcelMonthGroups` |  | [{yearMonthLabel, startDayIndex, dayCount}, ...]（startDayIndexはtimelineDays内の | — | 基本設計書 | ○ |
| 3705 | `stripHashFromHexColor` |  | "#"無しの6桁16進数文字列を返す。 | — | 基本設計書 | ○ |
| 3716 | `buildGanttSheet` |  | xlsx-js-styleのワークシートオブジェクトを返す。 | — | 基本設計書・対応表 | ○ |
| 3785 | `buildSummarySheet` |  | xlsx-js-styleのワークシートオブジェクトを返す。 | — | 基本設計書・詳細設計書・対応表 | ○ |
| 3808 | `exportExcel` | async | 完了後、トースト「Excelファイルを保存しました」を表示する。 | — | 詳細設計書 | ○ |
| 3828 | `calculateTodayMarkerPosition` |  | 範囲内ならleftPx（数値）、範囲外（表示中のプロジェクト期間の外）ならnullを返す。 | — | データモデル設計 | ○ |
| 3840 | `calculateBarGeometry` |  | {leftPx, widthPx, durationDays} を返す。日付が無い場合はnullを返す。 | — | データモデル設計 | ○ |
| 3853 | `shiftDateStringByDays` |  | 計算後の日付文字列を返す。 | — | 詳細設計書 | ○ |
| 3872 | `calculateDraggedBarDates` |  | クランプ済みの{startDate, endDate}を返す。 | — | 詳細設計書 | ○ |
| 3907 | `calculateMilestoneMarkerPositions` |  | {leftPx, name}[] を返す。 | — | 詳細設計書 | ○ |
| 3921 | `collectIssueSubtreeIds` |  | [rootIssueId, ...子孫id...] の配列を返す。 | — | — | ○ |
| 3950 | `filterVisibleIssues` |  | 実際に画面へ表示すべきissueだけの配列を返す。 | — | — | ○ |
| 3967 | `buildMindmapEdgeList` |  | {fromIssue, toIssue}[] を返す。 | — | — | ○ |
| 3984 | `calculateEdgeGeometry` |  | {leftPx, topPx, widthPx, angleDeg} を返す。 | — | — | ○ |

### 追加実装仕様書8.3節：マインドマップの自動レイアウト計算

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 4000 | `calculateMindmapNodeHeight` |  | ノードの深さ（depthLevel）別の想定高さ（お手本のnodeH関数に相当）。 | — | — |  |
| 4010 | `calculateMindmapHorizontalOffset` |  | 深さ（depthLevel）別の親子間の水平間隔（お手本のxOffに相当）。 | — | — |  |
| 4025 | `buildMindmapChildIssuesByParentId` |  | Map<issueId, issue[]>を返す（子を持たないissueはキーとして現れない）。 | — | — | ○ |
| 4040 | `assignMindmapDepthLevels` |  | Map<issueId, depthLevel>を返す。 | — | — | ○ |
| 4042 | `assignDepth` |  | — | — | — |  |
| 4057 | `calculateMindmapSubtreeHeight` |  | このノードに必要な高さ（px）を返す。subtreeHeightByIssueIdに全ノード分書き込む。 | — | — | ○ |
| 4080 | `layoutMindmapSubtree` |  | positionByIssueIdに{x,y}が書き込まれる（戻り値なし）。 | — | — | ○ |
| 4110 | `calculateMindmapAutoLayoutPositions` |  | issueId→{x,y} のMapを返す。ルートissueが無ければ空のMapを返す | — | — | ○ |
| 4139 | `applyMindmapAutoLayout` | async | ルートから辿れる全issueのx/yが最新のツリーレイアウトに更新される。 | — | — | ○ |
| 4155 | `calculateIssueDepthLevel` |  | depthLevel（整数）。issueIdが見つからない場合は0を返す。 | — | — | ○ |

### 追加実装仕様書8.3節：画面フィット

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 4177 | `calculateMindmapBoundingBox` |  | {minX, maxX, minY, maxY}。issueListが空ならnullを返す。 | — | — | ○ |
| 4197 | `handleMindmapFitViewButtonClick` | async | mindmapZoomLevelが更新され、キャンバスが再描画・再スクロールされる。 | — | — | ○ |
| 4225 | `renderProjectOptionsToHtml` |  | <select>のinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | 基本設計書 | ○ |
| 4255 | `renderWbsPanelToHtml` |  | wbsPanel要素のinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | 基本設計書 | ○ |
| 4311 | `calculateQuarterNumber` |  | 四半期番号を返す。 | — | — | ○ |
| 4335 | `buildGanttTimelineHeaderGroups` |  | なぜtimelineDaysは常に「日」単位のままなのか | — | UIデザイン仕様書 | ○ |
| 4436 | `renderGanttTimelineHeaderToHtml` |  | マイルストーンのひし形＋名前ラベルは月・日の見出し行の「上」に専用の帯として 描画する（追加実装仕様書4章） | — | UIデザイン仕様書 | ○ |
| 4494 | `renderGanttBodyToHtml` |  | スケジュール0件でも列は必ずプロジェクト全期間ぶん描画する | — | 詳細設計書・UIデザイン仕様書 | ○ |
| 4613 | `renderGanttChartToHtml` |  | ganttPanelBodyのinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | 詳細設計書・データモデル設計 | ○ |
| 4642 | `renderKanbanColumnToHtml` |  | renderKanbanBoardToHtml から列単位で呼び出されるHTML文字列を返す。 | — | 詳細設計書 | ○ |
| 4701 | `renderKanbanBoardToHtml` |  | カンバンパネルのinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | — | ○ |
| 4725 | `renderMindmapCanvasToHtml` |  | マインドマップキャンバスのHTML文字列を返す。 | — | — | ○ |

### UI：DOM反映（薄い関数）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 4758 | `applyProjectSelectOptions` |  | — | — | — |  |
| 4762 | `applyWbsPanel` |  | — | — | — |  |
| 4766 | `applyGanttPanel` |  | — | — | — |  |
| 4770 | `applyKanbanPanel` |  | — | — | — |  |
| 4774 | `applyMindmapPanel` |  | — | — | 基本設計書 |  |
| 4784 | `applyLockState` |  | 画面上のロック関連の見た目が状態に一致する。 | — | 基本設計書 | ○ |
| 4796 | `applyReportButtonAvailability` |  | 選択されていればdisabledを外し、されていなければdisabledにする。 | — | 基本設計書 | ○ |

### UI：統括（データ取得→純粋関数→DOM反映のつなぎ）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 4806 | `refreshProjectSelectDropdown` | async | <select>の中身が最新のプロジェクト一覧に更新される。 | — | 対応表 | ○ |
| 4822 | `rerenderWbsAndGanttFromCache` |  | WBS・ガント両パネルの表示と、全て開く/閉じるボタンのラベルが、現在のキャッシュ・ | — | 対応表 | ○ |
| 4854 | `refreshGanttPanel` | async | WBS・ガント両パネルの表示が最新の状態に更新される。 | — | — | ○ |
| 4873 | `applyGanttEmptyStateVisibility` |  | isEmptyがtrueなら空状態画面を表示し通常表示を隠す。falseならその逆。 | — | — | ○ |
| 4879 | `refreshKanbanPanel` | async | refreshGanttPanelと同じ考え方。対象はtasks・カンバンボード。 | — | 詳細設計書 |  |
| 4893 | `handleDeleteTaskButtonClick` | async | 確認OK時、対象タスクが削除されカンバンパネルが再描画される。 | — | 詳細設計書 | ○ |
| 4919 | `handleClearDoneTasksButtonClick` | async | 確認OK時、完了済みタスクがすべて削除されカンバンパネルが再描画される。 | — | 詳細設計書 | ○ |

### P2：スナップショットパネル

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 4983 | `compareSnapshot` | async | { add: [schedule...], mod: [{before, after, changes}...], del: [schedule...] } を返す。 | — | — | ○ |
| 5008 | `renderSnapshotCompareResultToHtml` |  | 比較結果表示欄に入れられるHTML文字列を返す。 | — | — | ○ |
| 5055 | `renderSnapshotListToHtml` |  | snapshotPanelBodyのinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | — | ○ |
| 5083 | `applySnapshotPanel` |  | — | — | — |  |
| 5090 | `updateSnapshotCountBadge` |  | #snapshotCountBadgeの表示テキストが最新の件数に切り替わる。 | — | — | ○ |
| 5098 | `refreshSnapshotPanel` | async | スナップショットパネルの表示・件数表示が最新の状態に更新される。 | — | 詳細設計書・データモデル設計・モーダル・ダイアログ一覧 | ○ |
| 5115 | `handleSaveSnapshotButtonClick` | async | 保存後、パネルを再描画し、トースト「スナップショットを保存しました」を | — | 詳細設計書・データモデル設計・モーダル・ダイアログ一覧 | ○ |
| 5142 | `handleDeleteSnapshotButtonClick` | async | 確認OK時、削除してパネルを再描画する。 | — | モーダル・ダイアログ一覧 | ○ |
| 5173 | `restoreSnapshot` | async | DBが復元後の状態になる（画面更新・トースト表示は呼び出し側の責務）。 | — | — | ○ |
| 5199 | `handleRestoreSnapshotButtonClick` | async | 確認OK時、DBが復元され、switchToProjectで画面一式（ガント・カンバン・ | — | 基本設計書 | ○ |
| 5216 | `handleCompareSnapshotButtonClick` | async | パネルが再描画され、対象スナップショットの比較結果が表示/非表示になる。 | — | 基本設計書 | ○ |

### P3：即時メモパネル

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 5237 | `renderQuickMemoTimelineToHtml` |  | quickMemoPanelBodyのinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | — | ○ |
| 5258 | `applyQuickMemoPanel` |  | — | — | — |  |
| 5262 | `refreshQuickMemoPanel` | async | — | — | — |  |
| 5277 | `handlePostQuickMemoButtonClick` | async | 投稿後、パネルを再描画する。 | — | 基本設計書 | ○ |
| 5294 | `handleDeleteQuickMemoButtonClick` | async | 削除後、パネルを再描画する。 | — | 基本設計書・詳細設計書 | ○ |
| 5305 | `handleClearQuickMemosButtonClick` | async | 確認OK時、このプロジェクトの即時メモがすべて削除され、パネルが再描画される。 | — | 詳細設計書 | ○ |

### P4：メモパネル

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 5339 | `renderMemoListToHtml` |  | memoListBodyのinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | 基本設計書 | ○ |
| 5378 | `renderMemoMarkdownToSafeHtml` |  | DOMにそのまま挿入できるサニタイズ済みHTML文字列を返す。 | — | — | ○ |
| 5392 | `renderMemoEditorToHtml` |  | memoEditorBodyのinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | — | ○ |
| 5424 | `applyMemoListPanel` |  | — | — | — |  |
| 5428 | `applyMemoEditorPanel` |  | — | — | — |  |
| 5437 | `refreshMemoPanel` | async | メモ一覧・編集エリア両方の表示が最新の状態に更新される。 | — | — | ○ |
| 5456 | `handleMemoEditorTabButtonClick` |  | 編集エリアの表示が切り替わる。 | — | — | ○ |
| 5461 | `handleCreateMemoButtonClick` | async | — | — | — |  |
| 5482 | `handleSelectMemoButtonClick` |  | — | — | 詳細設計書・モーダル・ダイアログ一覧 |  |
| 5491 | `handleDeleteMemoButtonClick` | async | 確認OK時、削除してパネルを再描画し（選択解除はrefreshMemoPanel側で判定）、 | — | 詳細設計書・モーダル・ダイアログ一覧 | ○ |
| 5509 | `handleMemoEditorFieldBlur` | async | 保存後、一覧側の表示（タイトル・更新日時）を最新化する。タイトル・本文の | — | — | ○ |

### P5：コメント一覧パネル

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 5541 | `renderCommentGroupsToHtml` |  | commentPanelBodyのinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | UI差分表 | ○ |
| 5576 | `renderCommentPostFormToHtml` |  | コメント投稿フォームのHTML文字列を返す。 | — | — | ○ |
| 5585 | `applyCommentPanel` |  | — | — | — |  |
| 5594 | `updateCommentCountDisplays` |  | #commentCountBadge・#commentPanelSummaryの表示が更新される。 | — | — | ○ |
| 5605 | `refreshCommentPanel` | async | コメントパネルの表示・件数表示が最新の状態に更新される。 | — | — | ○ |
| 5629 | `handleToggleCommentGroupButtonClick` |  | 対象グループのコメント本文一覧が表示/非表示になる。 | — | — | ○ |
| 5641 | `handleDeleteCommentButtonClick` | async | 削除後、パネルを再描画する。 | — | — | ○ |
| 5650 | `handlePostCommentButtonClick` | async | 投稿後、パネルを再描画する。 | — | — | ○ |

### P6：変更履歴パネル

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 5672 | `renderChangelogChangesToHtml` |  | 変更が無ければ空文字列、あれば一覧のHTML文字列を返す。 | — | UI差分表 | ○ |
| 5694 | `renderChangelogEntriesToHtml` |  | changelogPanelBodyのinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | UI差分表 | ○ |
| 5726 | `applyChangelogPanel` |  | — | — | 基本設計書 |  |
| 5734 | `renderChangelogSummaryToHtml` |  | 「全{n}件（追加{n}・変更{n}・削除{n}）」の文字列を返す。 | — | 基本設計書 | ○ |
| 5741 | `applyChangelogSummary` |  | — | — | — |  |
| 5749 | `refreshChangelogPanel` | async | 変更履歴パネルの表示が最新の状態に更新される。 | — | 詳細設計書 | ○ |
| 5764 | `handleClearChangelogButtonClick` | async | 確認OK時、このプロジェクトの変更履歴がすべて削除され、パネルが再描画される。 | — | 詳細設計書 | ○ |
| 5779 | `handleChangelogFilterClick` |  | 変更履歴パネルの表示が新しいフィルタの内容になる。 | — | データモデル設計 | ○ |
| 5792 | `getSnapshotDisplayName` |  | 表示用の名称文字列を返す。 | — | データモデル設計 | ○ |
| 5807 | `handleRestoreFromLogButtonClick` | async | snapshotが無ければ案内のみ表示し何もしない。復元後は対象パネルと | — | データモデル設計 | ○ |
| 5842 | `refreshMindmapPanel` | async | ルートノード＝プロジェクト名（追加実装仕様書8.2節） | — | — |  |
| 5871 | `switchToProject` | async | currentSelectedProjectId・currentProjectObjectが更新され、画面全体が新しい | — | モーダル・ダイアログ一覧 | ○ |
| 5896 | `toggleSlidePanel` |  | パネルが開いていれば閉じ、閉じていれば開く。 | — | モーダル・ダイアログ一覧 | ○ |
| 5903 | `closeSlidePanel` |  | パネルが閉じる。 | — | — | ○ |

### サイドパネルのリサイズハンドル（UI差分表No.76）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 5925 | `initializePanelResizeHandles` |  | 各パネルの左端にリサイズハンドルが表示される。 | — | — | ○ |
| 5936 | `handlePanelResizePointerDown` |  | リサイズ状態が開始される（実際の幅変更はpointermoveハンドラが行う）。 | — | — | ○ |
| 5952 | `handlePanelResizePointerMove` |  | パネルの見た目の幅がポインタに追従する。 | — | — | ○ |
| 5965 | `handlePanelResizePointerUp` |  | ドラッグが終了する（幅はその時点の値のまま維持される。DB保存はしない＝ | — | — | ○ |

### 共通UI：トースト通知（UIデザイン仕様書2.4節）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 5986 | `showToast` |  | TOAST_VISIBLE_DURATION_MS後にshowクラスが外れ、トーストが消える（戻り値なし）。 | — | — | ○ |

### UI：モーダル（モーダル・ダイアログ一覧①：中央モーダル）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 6006 | `openModal` |  | 画面中央にモーダルが表示される。 | — | — | ○ |
| 6014 | `closeModal` |  | モーダルが非表示になる。 | — | モーダル・ダイアログ一覧 | ○ |
| 6025 | `renderProjectModalToHtml` |  | モーダルパネルにそのまま入れられるHTML文字列を返す。 | — | モーダル・ダイアログ一覧 | ○ |
| 6060 | `openProjectModal` |  | モーダルが操作可能な状態で表示される。 | — | — | ○ |
| 6066 | `handleCreateProjectButtonClick` |  | — | — | — |  |
| 6070 | `handleEditProjectButtonClick` |  | — | — | — |  |

### M3：マイルストーン管理モーダル

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 6092 | `renderMilestoneModalRowsToHtml` |  | #milestoneModalRowsContainerの中身にそのまま入れられるHTML文字列を返す。 | — | 基本設計書・モーダル・ダイアログ一覧 | ○ |
| 6108 | `renderMilestoneModalToHtml` |  | モーダルパネルにそのまま入れられるHTML文字列を返す。 | — | モーダル・ダイアログ一覧 | ○ |
| 6124 | `syncMilestoneModalDraftRowsFromDom` |  | milestoneModalDraftRowsが画面表示と同じ内容になる。 | — | — | ○ |
| 6135 | `rerenderMilestoneModalRows` |  | 行一覧の表示が作業用コピーと一致する。 | — | 基本設計書 | ○ |
| 6143 | `handleAddMilestoneRowButtonClick` |  | 行一覧に空行が1行増える。 | — | 基本設計書 | ○ |
| 6152 | `handleRemoveMilestoneRowButtonClick` |  | 行一覧から指定行が消える。 | — | 詳細設計書 | ○ |
| 6167 | `handleMilestoneModalSaveButtonClick` | async | 保存後、モーダルを閉じてガントパネルを再描画し、 | — | 詳細設計書 | ○ |
| 6186 | `openMilestoneModal` |  | マイルストーン管理モーダルが操作可能な状態で表示される。 | — | 基本設計書・モーダル・ダイアログ一覧 | ○ |
| 6212 | `validateProjectModalInput` |  | エラーがあればそのアラート文言（string）を、無ければnullを返す。 | — | 基本設計書・詳細設計書・データモデル設計・モーダル・ダイアログ一覧 | ○ |
| 6233 | `handleProjectModalSaveButtonClick` | async | 保存成功時はモーダルを閉じ、プロジェクト一覧・選択状態を更新し、 | — | 詳細設計書・データモデル設計 | ○ |
| 6277 | `deleteProjectCascade` | async | プロジェクトと配下データがすべて削除される。 | — | 詳細設計書・データモデル設計・モーダル・ダイアログ一覧 | ○ |
| 6367 | `handleDeleteProjectButtonClick` | async | 確認OK時、プロジェクトと配下データが削除され、画面が更新される。 | — | 基本設計書・詳細設計書・モーダル・ダイアログ一覧 | ○ |
| 6394 | `renderModalCommentSectionToHtml` |  | モーダルパネルにそのまま入れられるHTML文字列を返す。 | — | 基本設計書・モーダル・ダイアログ一覧 | ○ |
| 6423 | `renderScheduleModalToHtml` |  | — | — | — |  |
| 6496 | `openScheduleModal` | async | モーダルが操作可能な状態で表示される。 | — | — | ○ |
| 6522 | `handlePostModalCommentButtonClick` | async | 投稿後、同じ編集モーダルを再度開き直して最新のコメント一覧を表示する。 | — | — | ○ |
| 6534 | `handleDeleteModalCommentButtonClick` | async | 削除後、同じ編集モーダルを再度開き直す。 | — | — | ○ |
| 6539 | `handleAddScheduleButtonClick` |  | — | — | 基本設計書 |  |
| 6554 | `handleAddChildScheduleButtonClick` |  | 子スケジュール作成モーダルが表示される。 | — | 基本設計書・モーダル・ダイアログ一覧 | ○ |
| 6567 | `handleWbsRowClick` |  | 編集モーダルが表示される。 | — | モーダル・ダイアログ一覧 | ○ |
| 6577 | `validateScheduleModalInput` |  | エラーがあればそのアラート文言（string）を、無ければnullを返す。 | — | 基本設計書・詳細設計書・データモデル設計・モーダル・ダイアログ一覧 | ○ |
| 6614 | `handleScheduleModalSaveButtonClick` | async | 保存成功時はモーダルを閉じ、ガントパネルを更新し、トースト「保存しました」を | — | 基本設計書・詳細設計書 | ○ |
| 6686 | `handleMoveScheduleOrderClick` | async | 隣接する2件のorderが入れ替わり、ガントパネルが再描画され、 | — | 基本設計書・詳細設計書 | ○ |
| 6720 | `handleScheduleDragDrop` | async | 兄弟内の並び順が変わり、ガントパネルが再描画され、 | — | 基本設計書・詳細設計書 | ○ |

### ガントバーのドラッグによる日程変更（詳細設計書3.2.4）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 6780 | `collectScheduleDescendants` |  | 子孫スケジュールオブジェクトの配列を返す（子孫がなければ空配列）。 | — | 詳細設計書 | ○ |
| 6810 | `handleBarPointerDown` |  | ドラッグ状態が開始される（実際の見た目更新はpointermoveハンドラが行う）。 | — | 詳細設計書 | ○ |
| 6833 | `handleBarPointerMove` |  | バーの見た目がポインタの位置に追従する。 | — | 詳細設計書 | ○ |
| 6861 | `handleBarPointerUp` | async | DBが更新され、ガントパネルが再描画される。実際に日付が変わった場合のみ | — | 詳細設計書 | ○ |

### ガントバー直接クリックの軽量ポップアップ（UI差分表No.75／対応表No.17）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 6931 | `renderTaskPopupCommentSectionToHtml` |  | ポップアップのコメント欄に入れられるHTML文字列を返す。 | — | — | ○ |
| 6963 | `renderTaskPopupToHtml` |  | #taskPopupのinnerHTMLにそのまま入れられるHTML文字列を返す。 | — | — | ○ |
| 6981 | `closeTaskPopup` |  | ポップアップが非表示になる。 | — | — | ○ |
| 6993 | `openTaskPopup` | async | ポップアップがクリック位置の近くに表示される。 | — | — | ○ |
| 7009 | `handlePostTaskPopupCommentButtonClick` | async | 投稿後、コメント一覧が更新される。 | — | — | ○ |
| 7024 | `handleDeleteTaskPopupCommentButtonClick` | async | 削除後、コメント一覧が更新される。 | — | — | ○ |
| 7034 | `refreshTaskPopupCommentSection` | async | コメント一覧の表示が最新の状態になる（進捗・メモ欄は再構築されない）。 | — | 詳細設計書 | ○ |
| 7047 | `handleDeleteScheduleButtonClick` | async | 確認OK時、対象と子孫が削除されガントパネルが再描画され、トースト「削除しました」を | — | 詳細設計書・対応表 | ○ |
| 7070 | `handleToggleAllScheduleRowsButtonClick` |  | WBS・ガント両パネルが再描画され、ボタンのラベルも最新の状態に更新される。 | — | 対応表 | ○ |
| 7086 | `handleWbsRowIconClick` | async | actionに応じた処理が実行される。 | — | — | ○ |

### UI：その他のイベントハンドラ

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 7137 | `handleOpenKanbanAddFormButtonClick` | async | その列だけ追加フォームが開いた状態になり、タイトル入力欄にフォーカスが移る。 | — | — | ○ |
| 7147 | `handleCloseKanbanAddFormButtonClick` | async | 開いていた追加フォームが閉じ、「＋追加」ボタンの表示に戻る。 | — | モーダル・ダイアログ一覧 | ○ |
| 7157 | `handleKanbanAddFormSubmit` | async | 保存後、フォームを閉じてから（追加実装仕様書6.1節：保存後は自動的に閉じる | — | 詳細設計書・モーダル・ダイアログ一覧 | ○ |
| 7194 | `renderTaskModalToHtml` |  | モーダルパネルにそのまま入れられるHTML文字列を返す。 | — | 詳細設計書 | ○ |
| 7233 | `openTaskEditModal` |  | モーダルが操作可能な状態で表示される。 | — | — | ○ |
| 7245 | `handleTaskModalSaveButtonClick` | async | 保存成功時はモーダルを閉じ、カンバンパネルを更新する。 | — | — | ○ |
| 7273 | `handleOpenTaskEditModalClick` | async | 編集モーダルが表示される。 | — | 基本設計書・詳細設計書 | ○ |
| 7289 | `handleTaskDragDrop` | async | ステータスが実際に変わっていれば保存してカンバンパネルを再描画する。同じ列への | — | 基本設計書・詳細設計書 | ○ |

### 追加実装仕様書8.1節：マインドマップのインライン編集

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 7315 | `createMindmapChildNodeWithInlineEdit` | async | キャンバス上に無題ノードが出現し、タイトル入力可能な状態になる。 | — | — | ○ |
| 7345 | `handleMindmapInlineEditInputBlur` | async | インライン編集状態を解除し、パネルを再描画する。 | — | — | ○ |
| 7371 | `handleMindmapInlineEditInputKeydown` |  | inputからフォーカスが外れ、handleMindmapInlineEditInputBlurが呼ばれる。 | — | — | ○ |
| 7387 | `attachMindmapInlineEditInputEvents` |  | インライン編集中の<input>が操作可能になる。 | — | — | ○ |
| 7403 | `handleAddIssueButtonClick` | async | createMindmapChildNodeWithInlineEditと同じ。 | — | — | ○ |

### マインドマップ：ノードのドラッグ配置

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 7435 | `handleMindmapNodePointerDown` |  | ドラッグ状態が開始される（実際の座標更新はpointermoveハンドラが行う）。 | — | — | ○ |
| 7449 | `handleMindmapNodePointerMove` |  | ノードが視覚的にポインタへ追従する。 | — | — | ○ |
| 7464 | `handleMindmapNodePointerUp` | async | ノードの新しい座標がDBに保存され、パネルが再描画される。 | — | — | ○ |

### マインドマップ：右クリックメニュー

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 7498 | `openMindmapContextMenu` |  | 右クリックメニューが画面上に表示される。 | — | — | ○ |
| 7509 | `closeMindmapContextMenu` |  | 右クリックメニューが閉じる。 | — | モーダル・ダイアログ一覧 | ○ |
| 7523 | `handleRenameIssueButtonClick` | async | 保存後、変更履歴を記録し、パネルを再描画する。 | — | モーダル・ダイアログ一覧 | ○ |
| 7539 | `handleAddChildIssueButtonClick` | async | createMindmapChildNodeWithInlineEditと同じ。 | — | モーダル・ダイアログ一覧 | ○ |
| 7550 | `handleAddSiblingIssueButtonClick` | async | createMindmapChildNodeWithInlineEditと同じ。 | — | モーダル・ダイアログ一覧 | ○ |
| 7563 | `handleMindmapContextMenuAction` | async | 選択した操作が実行される。 | — | 詳細設計書 | ○ |
| 7587 | `handleDeleteIssueButtonClick` | async | 確認OK、または3件以下ならdeleteIssueCascadeで削除し、自動レイアウトを | — | 詳細設計書 | ○ |
| 7604 | `handleToggleIssueCollapseButtonClick` |  | 対象ノードの子孫が表示/非表示になる。 | — | 基本設計書・詳細設計書 | ○ |
| 7618 | `handleLockToggleButtonClick` | async | ロック状態がDBに保存され、画面にも反映される。トースト「ロックしました」／ | — | 基本設計書・詳細設計書 | ○ |
| 7634 | `handleTodayButtonClick` |  | ガントチャートが今日の日付付近までスクロールされる。 | — | 基本設計書 | ○ |

### WBSパネルとガント本体の縦スクロール同期（対応表No.41）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 7657 | `handleWbsPanelVerticalScroll` |  | ガント本体がWBSパネルと同じ縦位置までスクロールされる。 | — | — | ○ |
| 7669 | `handleGanttScrollAreaVerticalScroll` |  | WBSパネルがガント本体と同じ縦位置までスクロールされる。 | — | — | ○ |

### ガント背景ドラッグによる自動スクロール（対応表No.42）

| 行 | 関数 | async | 目的 | 直接触るストア | 参照設計書 | 証明構造 |
|----|------|-------|------|-----------|-----------|---------|
| 7701 | `handleGanttBackgroundPointerDown` |  | 背景ドラッグの状態が開始される（実際のスクロールはpointermoveハンドラが行う）。 | — | 対応表 | ○ |
| 7716 | `handleGanttBackgroundPointerMove` |  | ガント本体（および対応表No.41の同期によりWBSパネルも）がポインタに追従してスクロールする。 | — | 対応表 | ○ |
| 7728 | `handleGanttBackgroundPointerUp` |  | 背景ドラッグが終了する。 | — | — | ○ |
| 7738 | `handleGranularityButtonClick` |  | ガントチャートが新しい粒度の見出し・列幅で再描画される。 | — | — | ○ |
| 7751 | `handleToggleColumnClick` |  | WBSパネルに列が追加/削除された状態が反映される。 | — | — | ○ |
| 7762 | `handleSwitchToKanbanButtonClick` |  | タスク管理パネルの開閉が行われる。 | — | — | ○ |
| 7774 | `handleOutOfScopeButtonClick` |  | アラートで機能名とスコープ外である旨を表示する（データは変更しない）。 | — | — | ○ |
| 7784 | `initializeApp` | async | 画面を開いた直後から、先頭プロジェクトのデータが3パネルに表示された状態になる | — | モーダル・ダイアログ一覧・UI差分表 | ○ |

## 3. ストア別・逆引き（どの関数がどのストアを直接触るか）

「このストアに関わる処理はどこ？」を引くための逆引き表。データの流れを追うときの入口になる。

### `projects`（3 関数）

`addProject`(L2073), `getAllProjects`(L2085), `deleteProject`(L2096)

### `schedules`（6 関数）

`addSchedule`(L2116), `getSchedulesByProject`(L2129), `getChildSchedules`(L2144), `deleteSchedule`(L2159), `getAllSchedules`(L2171), `getScheduleById`(L2660)

### `tasks`（4 関数）

`addTask`(L2191), `getTasksByProject`(L2202), `deleteTask`(L2212), `getAllTasks`(L2222)

### `comments`（4 関数）

`addComment`(L2241), `getCommentsByTask`(L2255), `deleteComment`(L2268), `getAllComments`(L2279)

### `changelog`（3 関数）

`addChangelogEntry`(L2299), `getChangelogByProject`(L2311), `clearChangelogByProject`(L2330)

### `issues`（4 関数）

`addIssue`(L2361), `getIssuesByProject`(L2374), `deleteIssue`(L2387), `getAllIssues`(L2397)

### `memos`（4 関数）

`addMemo`(L2416), `getMemosByProject`(L2427), `deleteMemo`(L2438), `getAllMemos`(L2448)

### `quickmemos`（5 関数）

`addQuickMemo`(L2465), `getQuickMemosByProject`(L2476), `deleteQuickMemo`(L2487), `clearQuickMemosByProject`(L2503), `getAllQuickMemos`(L2527)

### `snapshots`（4 関数）

`addSnapshot`(L2546), `getSnapshotsByProject`(L2557), `deleteSnapshot`(L2570), `getAllSnapshots`(L2580)

